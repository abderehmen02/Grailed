"use client"

import { useQuery } from "@tanstack/react-query"

export const UserInfo = ()=>{
    // const {data , error , isLoading} = useQuery({
    //     queryFn = async ()=>{
    //         const data
    //     }
    // })
    return <div className="flex items-center between pageMaxWidth" >
        </div>
}